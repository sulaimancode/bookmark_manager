<h1> Links with tag: bubbles </h1>

<ul id='links'>
  <% @links.each do |link| %>
  <% if link.tags.map(&:name).include? 'bubbles' %>
    <li>
      Title: <%= link.title %>
      URL:   <%= link.url   %>
    </li>
  <% end %>
  <% end %>
</ul>
